[package]
name = "kamea-scheme"
version = "0.1.0"
edition = "2021"
description = "R4RS Scheme grounded in a 4KB Cayley table. no_std, zero deps."
license = "MIT"

[features]
default = ["std"]
std = []

# no_std by default when std feature is disabled
